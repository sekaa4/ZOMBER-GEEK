import { FC } from "react";
import classes from "./field-cell.module.scss";

const FieldCell: FC = () => {
  // const [st, useSt] = useState(0);
  return <div className={classes.fieldCell} />;
};

export default FieldCell;
